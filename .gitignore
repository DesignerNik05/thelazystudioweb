node_modules
dist
.DS_Store
*.log
hero-preview*.png
*preview*.png

# Claude Code — local only (rules/ IS tracked)
.claude/settings.local.json
.claude/launch.json

# Local env files — never commit real keys
.env
.env.*
!.env.example

# Build artifacts for upload
release/

# OG render intermediate
scripts/og/.og-raw.png
